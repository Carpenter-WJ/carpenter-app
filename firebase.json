{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "source": "functions",
    "predeploy": [
      "node -e \"require('fs').copyFileSync('pricing.js', 'functions/pricing.js')\""
    ]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
